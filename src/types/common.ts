import type { LucideIcon } from "lucide-react";

export type IconContent = {
  title: string;
  description: string;
  icon: LucideIcon;
};

export type NavItem = {
  label: string;
  href: string;
};
